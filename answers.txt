//to  all of the projects using one command in such a way that it is stored in .m2
//use the command mvn clean install
//to execute tests use the command mvn clean test
//the jar files are stored in the target folder of each project
//after upgrading the version of all projects (change it only in 1 place).
//the jar files are stored in the target folder of each project with the new version number